const functions = {
    divClickFunction: () => {
        let divPosition = event.target;
        console.log(divPosition);
        console.log("clicked!");
        return 0;
    },
}


export default functions;